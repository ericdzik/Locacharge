// lib/core/config/app_config.dart

class AppConfig {
  // Configuration pour les cartes OpenStreetMap (gratuit, pas besoin de clé API)
  // Les cartes utilisent OpenStreetMap via flutter_map

  // Si vous aviez d'autres clés (comme Google), elles pourraient être ici aussi.
  // static const String googleApiKey = 'YOUR_GOOGLE_API_KEY_HERE';
}
